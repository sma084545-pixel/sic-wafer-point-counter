# Sample data

Run `python scripts/generate_synthetic_wafer.py --all --output-dir sample_data/generated`
to create deterministic `clean`, `noisy`, and `difficult` synthetic wafers and
their independent ground-truth CSV files. Generated images are intentionally not
committed so test and demonstration runs remain reproducible.

